name: fernuni_discord_bot

services:

  scraper:
    # image: rndintusr/fuh-winfo-discordbot-grade-scraper:v1.0.0
    build:
      context: .
      dockerfile: scraper.Dockerfile
    user: appuser:appuser
    volumes:
      - ./data:/app/data
