[Unit]
Description=Run Docker Compose for myapp

[Service]
Type=oneshot
WorkingDirectory=/opt/myapp
ExecStart=/usr/bin/docker compose pull --quiet
ExecStart=/usr/bin/docker compose up -d
# Avoid parallel runs:
ExecStartPre=/usr/bin/flock -n /var/lock/myapp-compose.lock -c "echo locked"